import SearchBox from '../../src/components/SearchBox';

export default {
  title: 'Components/SearchBox',
  component: SearchBox,
  tags: ['autodocs'],
};

export const Default = (args) => <SearchBox {...args} />;
