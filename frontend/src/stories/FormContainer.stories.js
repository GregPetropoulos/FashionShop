import FormContainer from '../components/FormContainer';

export default {
  title: 'Components/FormContainer',
  component: FormContainer,
  tags: ['autodocs'],
};
export const Default = (args) => <FormContainer {...args} />;
